/**
 * @deprecated Import from `./imageNormalize.js` instead.
 * Kept as a compatibility shim for existing import paths.
 */
export {
  ImageImportError,
  ImageNormalizeError,
  convertHeicLikeToJpeg,
  isAcceptedImportFile,
  isHeicLikeFile,
  normalizeArtworkImage,
  prepareArtworkImageForSave,
} from './imageNormalize.js'
